"""Infrastructure — concrete adapters for the core ports.

AI, external APIs, storage and renderers live here. The core never depends on this
package (ADR-0001, Principle 7: components are replaceable).
"""
