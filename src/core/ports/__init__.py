"""Ports — the interfaces the core depends on (hexagonal architecture).

The core defines these abstractions; concrete adapters live in src/infrastructure/.
The core never imports infrastructure, AI or APIs.
"""
