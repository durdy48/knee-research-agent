#!/usr/bin/env bash
# kra — Knee Research Agent CLI launcher.
# Runs the MVP CLI without requiring installation.
set -euo pipefail
here="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec python3 "$here/src/cli/main.py" "$@"
